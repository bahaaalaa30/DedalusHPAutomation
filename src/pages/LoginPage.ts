import { Page } from '@playwright/test';
export class LoginPage { constructor(private readonly page: Page) {} private username = this.page.locator('#user-id'); private password = this.page.locator('#his-password'); private loginButton = this.page.locator('#common-login button'); private inlineError = this.page.getByText('Unauthorized use of this system'); async enterUsername(user: string) { await this.username.fill(user); return this; } async enterPassword(pass: string) { await this.password.fill(pass); return this; } async clickLogin() { await this.loginButton.click(); } async login(user: string, pass: string) { await this.enterUsername(user); await this.enterPassword(pass); await this.clickLogin(); } async getErrorMessage() { await this.inlineError.waitFor({ state: 'visible' }); return (await this.inlineError.innerText()).trim(); } }